#ifndef MENU_H
#define MENU_H

#include "SDL.h"

enum ActiveMenu {None, Main, Pause, GameOver, DrawMap, Win, Help};
enum Volume {NoSound, LowSound, NormalSound}

class Menu {
    public:
        bool hover(uint8_t button_id)
}

#endif